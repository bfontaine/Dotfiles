#! /bin/bash
#
# A quick script for https://github.com/tonsky/AnyBar
#
# Usage:
#   anybar <color>
#

__anybar() {
  local cmd="$1"
  [ -z "$cmd" ] && exit 1

  # aliases
  case "$cmd" in
    exit)
      cmd=quit
  esac

  echo -n "$cmd" | nc -4 -u -w 0 localhost 1738
}

__anybar $*
