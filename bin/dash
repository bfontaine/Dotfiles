#! /bin/bash
# :: Launch Dash from the terminal
# Usage:
#   dash <keyword> <query>
__dash() {
    local kw=$1; shift

    if [ ! -z "$kw" ]; then
        open "dash://$kw:$*"
    else
        echo 'usage:'
        echo '  dash <keyword> <query>'
    fi

}
__dash $*
