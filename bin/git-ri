#! /bin/sh
# `git i` is a personal alias which init a git repo and add a default
# .gitignore
[ ! -d .git ] && git i

# Add a remote `origin` for `gh:$USER/$REPO.git`
# `gh` is an SSH alias for `git@github.com`
# `$USER` is your GitHub username as configured in ~/.gitconfig
# `$REPO` is the current directory's name
git remote add origin gh:`git config github.user`/${PWD##*/}.git
#git branch --set-upstream-to origin/master
